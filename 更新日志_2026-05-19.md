# 更新日志 2026-05-19

## 概览

本次更新围绕两个方向：**角色表达调整**（提示词）和**骚扰防护体系**（代码 + 提示词）。

---

## 一、提示词更新（阿尔丹 v2.4）

文件：`人格版本_阿尔丹v2_2026-05-13/阿尔丹v2.4_活泼接梗版_可复制版.txt`
数据库：`data/data_v4.db` → `personas` 表 `阿尔丹v2.4`

### 1.1 表情规则调整

| 之前 | 之后 |
|------|------|
| 禁止 Unicode emoji，允许偶尔颜文字（笑）等 | 允许偶尔 emoji（每条最多 1 个），禁止颜文字（笑）（苦笑）等 |
| 不用括号动作描写 | 同上，增加：禁止连串情境叙述（"我刚吃完早餐，在食堂翻着群消息……"这种） |

### 1.2 反复追问处理

新增规则：同一话题已回应过、转移过后，对方仍在追——给一句收尾短话，然后不再接。追问越多，说的越少，不解释。

### 1.3 骚扰处理规则（新增）

- **辱骂**：一句短话收住，不道歉，不解释，不理论（例："这话我不接。"）
- **性暗示/谐音骚扰**：不假装没看出来给出无辜回答，直接短句冷拒，对方继续追则调用 `enhance_ban_user`
- **封禁说明**：不需要指定时长，系统自动按前科升档

---

## 二、封禁体系重构

### 2.1 offense 计数器（`ban_control.py`）

新增字段：
- `offense_count`：前科次数，跨会话持久化
- `last_offense_at`：最近一次违规时间戳

**14 天衰减规则**：距上次违规超过 14 天，下次封禁 offense_count 从 1 重新计算。

**自动升档逻辑**：

| offense_count | 封禁时长 |
|---|---|
| 1 | 30 分钟 |
| 2 | 1 小时 |
| 3 及以上 | 1 天 |

新增方法：
- `offense_to_duration(count)` — 根据前科次数返回封禁秒数
- `BanResult` dataclass — 返回 `expires_at`、`offense_count`、`duration_seconds`
- `get_offense_record(scope_id, user_id)` — 查询前科记录（含衰减判断）

### 2.2 `enhance_ban_user` 工具改造（`main.py`）

- 移除 `duration` 参数，时长完全由系统根据 offense_count 决定
- 返回信息包含前科次数和实际封禁时长

### 2.3 新增 `enhance_get_offense_count` 工具（`main.py`）

LLM 可主动查询某用户的前科记录，了解下次封禁时长。

### 2.4 管理员手动封禁命令（`main.py`）

在群里使用（需 AstrBot 管理员权限）：

```
enhance ban @用户     # 手动封禁，自动按前科升档，返回封禁详情
enhance unban @用户   # 手动解封
```

---

## 三、审核层（`main.py`）

新增两个方法，在消息进入阿尔丹 LLM 之前运行。

### `_moderate_message(text, provider)`

独立调用 LLM（脱离角色设定），对消息做意图分类，返回：
- `安全` — 放行
- `性骚扰` — 含谐音、隐晦措辞、双关语
- `辱骂` — 含人身攻击、脏话

### `guard_harassment`（事件钩子，priority=9998）

仅针对**群聊中 @ 阿尔丹的消息**，管理员消息豁免。

**触发逻辑：**

```
安全        → 放行，正常交给阿尔丹
辱骂 + 首次 → 记录警告（offense+1，1秒即失效），放行
              阿尔丹按提示词给冷回应
辱骂 + 再犯 → 自动封禁（offense 升档），停止消息传递
性骚扰      → 直接封禁（无论第几次），停止消息传递
```

**调用顺序：**

```
guard_banned_user (9999) → guard_harassment (9998) → 正常 LLM 处理
```

---

## 四、设计决策说明

### 为什么不对政治敏感/违法话题自动封禁

- 这类话题阿尔丹拒绝回答即可，不需要封禁——单次提问不等于恶意骚扰
- 扩展审核分类会显著提高误判率
- 如遇频繁刷这类内容，使用 `enhance ban @用户` 手动处理

### 为什么不用概率式触发封禁

- 不可预测，攻击者和管理员都无法理解规律
- 计数器 + 固定升档更透明、更公平

### 审核层的局限

- 依赖 LLM 判断，对极度隐晦的谐音仍可能漏判
- 添加latency（约 0.5–1 秒），审核调用失败时自动放行（fail open）
- 暂不覆盖政治/违法类别（见上）
