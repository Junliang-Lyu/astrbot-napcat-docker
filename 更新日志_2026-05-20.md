# 更新日志 2026-05-20

## 概览

本次更新围绕**主动发言机制排查与调优**，包含 enhance_mode 判定提示词重写、时段感知新增、历史上下文扩充，以及 proactive_chat 免打扰时段格式修复。

---

## 一、问题排查：昨日主动回复几乎未触发

### 1.1 enhance_mode（model_choice）侧

**根因：提示词判断偏保守 + 历史上下文不足**

- `model_history_messages: 1`：模型判断时只能看到 1 条额外历史，上下文严重不足，拿不准时默认 SKIP
- 原提示词全局基调为"宁可少说"，REPLY 与 SKIP 条件不对等，导致几千条消息几乎无一触发

### 1.2 proactive_chat（沉默倒计时）侧

**根因：重启后群沉默计时器不恢复 + 调度间隔过长**

- `group_timers` 为纯内存结构，重启即丢失，启动后必须等群里有新消息才能重置计时器
- 即使计时器正常触发（60 min 沉默），也只是安排 **120\~480 分钟后**再发消息，活跃群极易在等待窗口内被新消息取消任务
- `quiet_hours: "23:00-8:00"` 格式错误，`is_quiet_time()` 解析时 `int("23:00")` 抛 ValueError，免打扰功能完全失效

---

## 二、enhance_mode 配置调整

文件：`data/config/astrbot_plugin_astrbot_enhance_mode_config.json`

### 2.1 model_history_messages：1 → 6

扩充判断时可见的历史上下文，避免模型因信息不足而保守跳过。

### 2.2 model_choice_prompt 重写

主要变化：

| 维度 | 改前 | 改后 |
|------|------|------|
| 整体基调 | "宁可少说" | 中性，按时段和内容判断 |
| 时段感知 | 无 | 中午（11-14）/ 晚上（19-00:30）适当放宽；深夜（00:30-7:00）默认沉默 |
| REPLY 条件 | 4 条，较笼统 | 5 条，含时段加权说明 |
| SKIP 条件 | 7 条，含多个保守项 | 5 条，保留安全拦截，去掉过度保守项 |
| 安全拦截 | 套话、引导违规等 | 保留不变 |

**触发机制说明**（备忘）：
- 当前为纯 model_choice 模式，`possibility: 0.6` 在此模式下不生效
- 每积累 5 条消息触发一次模型判断，输出 REPLY 或 SKIP，无概率二次过滤
- 后续可考虑在 REPLY 后叠加一层概率滤波（活跃时段 ~80%，其他时段 ~40%），防止判断过松时连续刷屏

---

## 三、proactive_chat 配置修复

文件：`data/config/astrbot_plugin_proactive_chat_config.json`

### 3.1 quiet_hours 格式修复

```
改前："23:00-8:00"（解析失败，免打扰完全无效）
改后："1-8"（01:00-08:00 静默，现在真正生效）
```

**说明**：`is_quiet_time()` 仅支持整点格式（如 `"23-8"`），带分钟的格式会触发 ValueError 并返回 False，导致深夜也照常触发。修复后 1:00am 起静默至 8:00am。

---

## 四、未解决的结构性问题（待后续处理）

1. **proactive_chat 重启后群计时器不恢复**：`group_timers` 纯内存，重启后需等新消息才能重新启动沉默倒计时，启动期间主动发言完全静默
2. **proactive_chat 调度间隔偏长**：60 min 沉默触发后还需再等 120\~480 min，活跃群极难触发，可考虑调低至 0\~30 min
3. **情绪场景无即时响应**：model_choice 依赖 5 条消息才触发一次判断，有人情绪低落时存在延迟，且无优先级处理；后续可在 `on_group_message` 加即时情绪检测层
